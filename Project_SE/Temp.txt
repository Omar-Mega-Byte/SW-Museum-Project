<?php
$conn= new mysqli('localhost','root','','projectsw');
if ($conn->connect_error) {
die("Connection failed: " . $conn->connect_error);
}
$sql = "SELECT * FROM `learn`";
// The query() / mysqli_query() function performs a query against a database.
$result = mysqli_query($conn, $sql);
// التحقق من عدد النتائج
if ($result->num_rows > 0) {
// (fetch_assoc() ---> وظيفة هذه الدالة هي قراءة السجل من قاعدة البيانات ووضعه في مصفوفة حسب اسم العمود في قاعدة البيانات)
// نقوم بعرض البيانات للمتصفح
echo "<table>";
echo "<tr><th>Name</th><th>category</th><th>link</th><th>photoUrl</th></tr>";

while ($row = mysqli_fetch_assoc($result)) {
    echo "<tr>";
    echo "<td>" . $row['resoursename'] . "</td>";
    echo "<td>" . $row['category'] . "</td>";
    echo "<td>" . $row['link'] . "</td>";
    echo "<td>" . $row['photourl'] . "</td>";
    echo "</tr>";
}
echo "</table>";
} else {
echo "No results found.";
}Data/2339301.jpg
Data/2339295.jpg
$conn->close();
?>